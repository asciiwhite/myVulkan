language: cpp
sudo: required
os: linux
dist: trusty
compiler:
  - clang
  - gcc
before_script:
  - sudo apt-get -qq update
  - sudo apt-get install -y libxinerama-dev libxrandr-dev libxcursor-dev libxi-dev
script:
  - cmake .
  - make
notifications:
  email:
    recipients:
      - serialize.this@gmail.com
    on_success: change
    on_failure: always